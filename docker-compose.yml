version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - JWT_SECRET=cowork-dev-secret-change-me
      
      - DATABASE_URL=sqlite:///cowork.db?timeout=30
    volumes:
    
      - cowork-data:/app

volumes:
  cowork-data:
